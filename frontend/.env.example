NEXT_PUBLIC_API_URL=http://127.0.0.1:3001

# Only needed when the frontend runs as a separate container from the backend
# (e.g. docker-compose), where server-side (SSR) fetches must use the internal
# service name instead of the browser-facing URL above.
# API_INTERNAL_URL=http://backend:3001
